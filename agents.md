# AI Agent Guidelines

- Never use standard browser `alert()` or `window.confirm()` functions.
- Always implement custom prompts, inline states, or use our `toast` notifications for feedback and interactive prompts.
